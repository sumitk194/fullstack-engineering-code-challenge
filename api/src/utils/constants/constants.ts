export const API_PREFIX = 'api';

export const API_VERSIONS = Object.freeze({
  V1: 'v1',
});

export const CONTROLLERS = Object.freeze({
  RESULT: 'scan',
});


export const SWAGGER_INFO = Object.freeze({
  TITLE: 'API  ',
  VERSION: '1.0',
  ROUTE: 'api-docs',
});
